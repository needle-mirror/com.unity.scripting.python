# Make sure the PYTHONPATH gets sent down and interpreted.
import sys
import os
sys.stdout = open(os.path.join(sys.argv[1], 'stdout.txt'), 'w')
sys.stderr = open(os.path.join(sys.argv[1], 'stderr.txt'), 'w')

for x in sys.path:
    print(x)
